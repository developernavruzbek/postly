server:
  port: ${services.ports.interaction}
spring:
  application:
    name: interaction

  datasource:
    url: jdbc:postgresql://localhost:5432/postly
    username: postgres
    password: root123

  jpa:
    properties:
      hibernate:
        default_schema: interactions

    hibernate:
      ddl-auto: update

    show-sql: true
