server:
  port: ${services.ports.user}
spring:
  application:
    name: user

  datasource:
    url: jdbc:postgresql://localhost:5432/postly
    username: postgres
    password: root123

  jpa:
    properties:
      hibernate:
        default_schema: users

    hibernate:
      ddl-auto: update

    show-sql: true
